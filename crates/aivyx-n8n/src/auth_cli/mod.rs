//! Minimal CLI for `aivyx-n8n` — same slim shape as
//! `aivyx-notion`'s (no OAuth flow; just status + check).
//! Second data point validating the auth_cli substrate
//! lift candidate flagged at Phase 130 exit.

pub mod cli;
pub mod config_file;
pub mod status;
