# Rust
/target/
Cargo.lock.bak
**/*.rs.bk

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local env / secrets
.env
*.local

# Phase 99 — disposable local-run state from scripts/dev-run.sh
# (sandbox FS root + encrypted dev store; never real data)
/.dev-run/

# Local-only audit / scratch docs (not part of frozen phase journals)
/docs/walkthrough.md
