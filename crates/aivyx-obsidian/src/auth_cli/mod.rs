//! Minimal CLI for `aivyx-obsidian` — aligned with the
//! `aivyx-notion` + `aivyx-n8n` shape via the Phase 132
//! `aivyx-auth-cli` substrate lift. Offline `auth
//! status` + online `auth check`, both backed by the
//! shared substrate report types.

pub mod cli;
pub mod config_file;
pub mod status;
