//! Intentionally empty. This crate exists only so the workspace member glob
//! `crates/verticals-private/*` always resolves to a valid `Cargo.toml`,
//! including inside Dependabot's sparse checkout. It ships no functionality.
